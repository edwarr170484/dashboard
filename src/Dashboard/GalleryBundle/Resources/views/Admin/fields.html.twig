{% block collection_widget %}
    {{ block('form_widget') }}
{% endblock collection_widget %}

{% block form_widget_compound %}
    {{ block('form_rows') }}
{% endblock form_widget_compound %}

{% block _gallery_items_row %}
    <div class="col-lg-12 col-md-12 discussions">
        {{ form_widget(form) }}
    </div>
{% endblock _gallery_items_row %}

{%- block _gallery_items_entry_label -%}
    {% if label is not same as(false) -%}
        {% if not compound -%}
            {% set label_attr = label_attr|merge({'for': id}) %}
        {%- endif -%}
        {% if required -%}
            {% set label_attr = label_attr|merge({'class': (label_attr.class|default('') ~ ' required')|trim}) %}
        {%- endif -%}
        {% if label is empty -%}
            {%- if label_format is not empty -%}
                {% set label = label_format|replace({
                    '%name%': name,
                    '%id%': id,
                }) %}
            {%- else -%}
                {% set label = name|humanize %}
            {%- endif -%}
        {%- endif -%}
        {{ translation_domain is same as(false) ? label : label|trans({}, translation_domain) }}
    {%- endif -%}
{%- endblock _gallery_items_entry_label -%}

{% block _gallery_items_entry_row %}
                {{ form_widget(form) }}
                {{ form_widget(form) }}
                {{ form_widget(form) }}
                {{ form_widget(form) }}
                {{ form_widget(form) }}
                {{ form_widget(form) }}
                {{ form_widget(form) }}
                {{ form_widget(form) }}
                {{ form_widget(form) }}
{% endblock %} 

{% block form_row %}
        <div class="form-group">
            {{ form_label(form)}}
            {{ form_widget(form)}}
        </div>
{% endblock form_row %}